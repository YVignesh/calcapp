name: calc
description: "A sleek and comprehensive calculator app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.6.1
  provider: ^6.1.2
  math_expressions: ^2.6.0
  intl: ^0.20.1
  http: ^1.2.2
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  fl_chart: ^0.70.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
